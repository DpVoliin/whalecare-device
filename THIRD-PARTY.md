# 第三方与出处（THIRD-PARTY）

本项目的**自有代码**按 MIT 授权（见 `LICENSE`）。以下是依赖与资料的出处，
尤其是**为什么某些东西没有直接抄过来**。

## 运行时依赖

| 依赖 | 许可证 | 说明 |
|---|---|---|
| [LovyanGFX](https://github.com/lovyan03/LovyanGFX) | **FreeBSD**（主体）+ 内置字体的 BSD/MIT/IPA 各自授权 | 比 MIT 还宽松 → **可以**进 MIT 项目。内置 `efontCN`（3-clause BSD）让中文常用字能直接显示 |
| Arduino-ESP32 core | **LGPL-2.1** | 框架本身，不修改、不静态打包进本仓库源码 |
| ESP-IDF 组件 | Apache-2.0 / MIT 等 | 随 Arduino core |

> 选 LovyanGFX 而不是 Arduino_GFX：后者的许可证在仓库里**查不到明确声明**
> （GitHub 也识别为 NOASSERTION），拿不准的东西不进 MIT 项目。

## 资料出处（只取事实，不取代码）

| 事实 | 出处 | 为什么不算抄 |
|---|---|---|
| 屏幕/触摸/I2C/按键**引脚号** | LilyGo 官方仓库 `pin_config.h` | 引脚编号是**事实**（与"代码表达"不同），且必须与硬件一致，没有第二种写法 |
| ST7796 的**列偏移 49**、`IPS` 反相等初始化参数 | 同上仓库的示例 | 同样是硬件事实（写错就花屏） |
| 采集器侧 `/mcu/inbox` 协议 | 本项目姊妹仓库 [whalecare](https://github.com/DpVoliin/whalecare) | 同作者 |

**没有复制的东西**：LilyGo 仓库里的示例代码是 **GPL-3.0**，与本项目 MIT 不兼容，
所以这里**一行都没有抄**——所有绘制、网络、动画、按键逻辑都是本项目自己写的。
如果你要引用它们的代码，请注意那部分必须按 GPL-3.0 处理。

## 美术素材

**没有第三方素材。** 鲸鱼、气泡、状态栏全都是**代码画出来的**（圆、三角、圆角矩形），
所以可以安心开源 —— 这也是刻意的：素材版权是最容易让一个开源项目变得不可分发的东西。
